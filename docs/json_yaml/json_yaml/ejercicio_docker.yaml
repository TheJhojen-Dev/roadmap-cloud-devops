---
service:
  web_proxy:
    image: nginx:alpine
    container_name: production_nginx
    restart: always
    ports:
      - 80:80
      - 443:443
    environment:
      NGINX_HOST: api.local
      NGINX_PORT: 80
